export const actionCreatorIncrement = () => {
    let action = {
        type: "INCREMENT"
    }
    return action;
}

export const actionCreatorDecrement = () => {
    let action = {
        type: "DECREMENT"
    }
    return action;
}